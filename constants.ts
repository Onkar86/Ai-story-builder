
import { GenerationType, Genre } from './types';

export const GENERATION_TYPES: GenerationType[] = [
  GenerationType.PlotIdea,
  GenerationType.CharacterDescription,
  GenerationType.Dialogue,
  GenerationType.ExpandText,
  GenerationType.WorldBuilding,
];

export const GENRES: Genre[] = [
  Genre.Fantasy,
  Genre.SciFi,
  Genre.Mystery,
  Genre.Romance,
  Genre.Horror,
  Genre.Thriller,
  Genre.Comedy,
  Genre.Historical,
];
